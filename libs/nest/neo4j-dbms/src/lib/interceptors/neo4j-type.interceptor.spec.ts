import { Neo4jTypeInterceptor } from './neo4j-type.interceptor';

describe('Neo4jTypeInterceptor', () => {
  it('should be defined', () => {
    expect(new Neo4jTypeInterceptor()).toBeDefined();
  });
});
