overwrite: true
schema: 'http://localhost:4000'
generates:
  libs/data-access/cc/src/lib/generated.ts:
    documents: ['libs/graphql-types/src/lib/**/*.graphql']
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
