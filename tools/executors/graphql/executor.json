{
  "executors": {
    "emit": {
      "implementation": "../../../dist/tools/executors/graphql/impl",
      "schema": "./schema.json",
      "description": "Runs `echo` (to test executors out)."
    }
  }
}